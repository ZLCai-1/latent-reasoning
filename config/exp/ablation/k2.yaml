# =============================================================================
# 消融实验：K=2（2 个 latent tokens）
# =============================================================================

defaults:
  - ../stage1_transition

model:
  num_latent_tokens: 2

curriculum:
  enabled: false

logging:
  run_name: "ablation-k2"

checkpoint:
  save_dir: "checkpoints/ablation/k2"
