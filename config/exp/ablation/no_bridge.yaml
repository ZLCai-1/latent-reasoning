# =============================================================================
# 消融实验：去掉 bridge loss
# =============================================================================

defaults:
  - ../stage1_transition

loss:
  bridge_weight: 0.0

logging:
  run_name: "ablation-no-bridge"

checkpoint:
  save_dir: "checkpoints/ablation/no_bridge"
