# =============================================================================
# 消融实验：去掉 transition loss
# =============================================================================

defaults:
  - ../stage1_transition

loss:
  transition_weight: 0.0
  generation_weight: 1.0

logging:
  run_name: "ablation-no-transition"

checkpoint:
  save_dir: "checkpoints/ablation/no_transition"
