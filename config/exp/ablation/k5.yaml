# =============================================================================
# 消融实验：K=5（5 个 latent tokens）
# =============================================================================

defaults:
  - ../stage1_transition

model:
  num_latent_tokens: 5

curriculum:
  enabled: false

logging:
  run_name: "ablation-k5"

checkpoint:
  save_dir: "checkpoints/ablation/k5"
