# =============================================================================
# 消融实验：K=1（仅 1 个 latent token）
# =============================================================================

defaults:
  - ../stage1_transition

model:
  num_latent_tokens: 1

curriculum:
  enabled: false

logging:
  run_name: "ablation-k1"

checkpoint:
  save_dir: "checkpoints/ablation/k1"
