# =============================================================================
# 基线：Short CoT（限制推理链长度）
# =============================================================================

defaults:
  - ../stage0_cot

data:
  max_seq_length: 128  # 截断长 CoT

training:
  num_epochs: 5

logging:
  run_name: "baseline-short-cot"

checkpoint:
  save_dir: "checkpoints/baselines/short_cot"
